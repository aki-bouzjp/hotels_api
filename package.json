{
  "name": "hotels_api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:studio": "prisma studio",
    "prisma:format": "prisma format",
    "prisma:migrate": "prisma migrate",
    "prisma:migrate:dev": "dotenv -e .env.development -- npx prisma migrate dev --name postgres-init",
    "prisma:generate": "prisma generate"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "dotenv-cli": "^6.0.0",
    "prisma": "^4.8.0"
  },
  "dependencies": {
    "@prisma/client": "^4.8.0"
  }
}
